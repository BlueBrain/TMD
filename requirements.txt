#Requirements for neurontopology
#These requirements are assumed to be installed in the system.
#matplotlib>=1.3.1

#These can be installed with pip if necessary.
enum34>=1.0.4
numpy>=1.8.0
scipy>=0.13.3
